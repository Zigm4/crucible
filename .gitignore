# dependencies
node_modules/

# build outputs
dist/
.vite/

# system / editor noise
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# environment (this project doesn't use any, but a guard rail just in case)
.env
.env.*
!.env.example

# Claude Code local state
.claude/

# Etabli local de la collection : jamais commite, jamais construit.
# tsconfig ne couvre que src/ et vite.config ne liste pas cette page en entree,
# donc rien ici ne peut atteindre un bundle. Voir tools/actions.ts.
tools/
