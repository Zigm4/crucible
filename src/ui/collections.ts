/**
 * Collections offered as a starting point.
 *
 * Every screen that asks for a collection name faces the same problem:
 * a WAX collection is up to twelve characters of no vowels, and typing
 * one exactly from memory is where people give up. These are the ones
 * this page was built against and tested on, so they are the ones most
 * likely to show somebody what the tool does on their first try.
 *
 * Not a whitelist, not an endorsement, and not the only ones that work.
 * Anything can be typed into the box beside them.
 */
export const SUGGESTED_COLLECTIONS = [
  'underpunks55',
  'cigalepixeld',
  'northshireup',
  'pearlhorizon',
] as const;
